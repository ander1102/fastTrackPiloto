import { createViewManager } from "@app/components/ViewManager/View";

export const modalManager = createViewManager();

const ModalContainer = modalManager.component;

export default ModalContainer;
